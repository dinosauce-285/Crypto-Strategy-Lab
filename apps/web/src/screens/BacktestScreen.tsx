import { Header } from '../layout/Header';

/**
 * Placeholder — the real content is the historical-data work from the Module 1 gap
 * analysis (a date-range query and a swappable historical adapter), not yet built.
 */
export function BacktestScreen() {
  return (
    <main className="screen">
      <Header title="Backtest" />
      <div className="screen-body">
        <p className="state">Historical data — coming next.</p>
      </div>
    </main>
  );
}
