packages:
  - "apps/*"
  - "packages/*"

# Prisma needs its postinstall to fetch query engines; esbuild needs its binary.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
  esbuild: true
