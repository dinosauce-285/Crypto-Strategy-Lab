node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# env & secrets. Plaintext never lands in git; the age-encrypted copy under envs/ does.
**/.env
**/.env.*
!**/.env.example
.env.key
*.pem
*.key
credentials*.json

# tool config holding real tokens
.mcp.json
!.mcp.json.example

# local data
*.log
.DS_Store
.vscode/
.idea/
# (postgres data lives in a docker named volume, not the repo)

# prisma 7 generates the client into the source tree
apps/api/src/generated/

# agent runtime artefacts
.claude/
.codex/
.cursor/
.windsurf/
.agents/
.gemini/
CLAUDE.md
**/CLAUDE.md
GEMINI.md
**/GEMINI.md
.claude/plans/*
!.claude/plans/.gitkeep
.claude/journals/

# impeccable: DESIGN.md and its sidecar are shared, live-mode session state is not
**/.impeccable/live/sessions/

docker-compose.yml