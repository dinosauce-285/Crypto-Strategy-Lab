# Đối chiếu tính năng mockup với đề bài &amp; hiện trạng hệ thống

> Nguồn tính năng: `docs/images/ui-inventory.md` (liệt kê thuần tuý từ 5 ảnh mockup do AI vẽ
> tham khảo, KHÔNG phải đề bài gốc).
> Nguồn đối chiếu yêu cầu: `docs/crypto-strategy-lab-do-an-cuoi-ky.md` (đề bài gốc, đọc toàn
> văn mục 1-47).
> Nguồn đối chiếu hiện trạng: code thật trong repo (`apps/api/src`, `apps/web/src`,
> `prisma/schema.prisma`), có tham chiếu `docs/notes/bugs-and-findings.md` khi trùng vấn đề
> đã ghi nhận (không lặp lại chi tiết, chỉ dẫn tới đó).
>
> **Thang phân loại mức độ yêu cầu:**
>
> - **Must** — có tên trong mục 37 "Mức tối thiểu – MVP".
> - **Should** — được đề bài mô tả rõ ở phần thân (mục 1-36, 39-47) nhưng không có tên tường
> minh trong mục 37.
> - **Nice to have** — có tên trong mục 38 "Phần mở rộng".
> - **No need** — không xuất hiện ở bất kỳ đâu trong đề bài, không phục vụ driver kiến trúc
> nào ở mục 32/40 — nhiều khả năng do AI vẽ ảnh tự thêm.
>
> **Thang hiện trạng:** Có-đúng / Có-nhưng-có-vấn-đề / Chưa có — mỗi ô đều kèm bằng chứng
> file:dòng hoặc từ khoá grep đã dùng để xác nhận "chưa có".
>
> **Cột "Quyết định"**: để trống `[ ]` = chưa quyết; gõ `[x]` = **CẦN** implement; gõ `[-]` =
> **KHÔNG CẦN**. Tự chọn quy ước khác nếu muốn, miễn nhất quán để dễ tổng hợp lại.

---

## Thành phần chung + Màn hình 1: Realtime Chart


| Quyết định | Tính năng                                                                          | Mức độ yêu cầu (căn cứ đề bài)                                                                                                                                                                        | Hiện trạng hệ thống (bằng chứng)                                                                                                                                                                           |
| ---------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ ]        | Logo + tên app "Crypto Strategy Lab"                                               | **No need** — không mục nào yêu cầu logo/thương hiệu; không phục vụ driver kiến trúc nào ở mục 32/40.                                                                                                 | Có, hoạt động đúng — `apps/web/src/layout/Navbar.tsx:103` render `<p className="navbar-title">Crypto Strategy Lab</p>`.                                                                                    |
| [ ]        | Sidebar/Navbar điều hướng giữa các module chính                                    | **Should** — mục 45 "Demo" liệt kê chính các luồng cần demo (Realtime, Backtest, Leaderboard, News...), ngụ ý cần điều hướng; không có tên "navbar" tường minh trong mục 37.                          | Có, hoạt động đúng — `apps/web/src/layout/Navbar.tsx:91-97` có 5 tab: `/realtime`, `/backtest`, `/search`, `/leaderboard`, `/news`, khớp 5 file trong `apps/web/src/screens/`.                             |
| [ ]        | Mục menu "Strategy Engine" (tạo strategy từ prompt/URL)                            | Xem phân loại chi tiết ở bảng "Màn hình 2" bên dưới.                                                                                                                                                  | Chưa có làm tab/route riêng — grep `apps/web/src/screens/` không có `StrategyEngineScreen`; `Navbar.tsx:91-97` không có tab tên này.                                                                       |
| [X]        | Mục menu "Discovery"                                                               | Xem phân loại chi tiết ở bảng "Màn hình 3" bên dưới.                                                                                                                                                  | Chưa có route riêng tên "Discovery" — chỉ có `/search` (`SearchScreen.tsx`), không có `DiscoveryScreen`/route `/discovery`.                                                                                |
| [X]        | Mục menu "News Crawler"                                                            | Xem phân loại chi tiết ở bảng "Màn hình 5" bên dưới.                                                                                                                                                  | Có tab `/news` (`Navbar.tsx:96`) nhưng UI khác hẳn mockup (không có Website/RSS/HTML picker, không có Auto-refresh) — chi tiết ở bảng Màn hình 5.                                                          |
| [ ]        | Mục menu "Settings"                                                                | **No need** — không xuất hiện ở bất kỳ đâu trong đề bài, không phục vụ driver nào ở mục 32/40.                                                                                                        | Chưa có — 5 tab liệt kê ở trên không có "Settings"/route `/settings`.                                                                                                                                      |
| [ ]        | Badge gói cước "Pro Student" + hạn dùng + avatar/tên/email user                    | **No need** — không có yêu cầu subscription/tài khoản người dùng ở bất kỳ mục nào; đồ án không có khái niệm phân quyền/gói cước.                                                                      | Chưa có — grep `"Pro Student"`, `student@example.com` trong `apps/web/src/` không ra kết quả; `Navbar.tsx`/`Header.tsx` không có phần tử tài khoản/gói cước nào.                                           |
| [ ]        | Badge "Nguồn dữ liệu: Binance API + WebSocket" trên mọi trang                      | **No need** — đề bài yêu cầu *dùng* Binance qua adapter (mục 4, mục 37) nhưng không yêu cầu hiển thị badge nguồn dữ liệu; không phải driver ở mục 32/40.                                              | Chưa có — grep chuỗi hiển thị `"Binance API"`, `"WebSocket"` trong `apps/web/src/` không ra kết quả; `Header.tsx`/`Navbar.tsx` không có component này.                                                     |
| [ ]        | Icon help (dấu hỏi) góc trên phải                                                  | **No need** — không có yêu cầu trợ giúp/help UI trong đề bài.                                                                                                                                         | Chưa có — `Header.tsx` (toàn bộ, 11 dòng) chỉ render `<h1>{title}</h1>`, không có icon help.                                                                                                               |
| [ ]        | Icon chuông thông báo                                                              | **No need** — không có yêu cầu notification UI; event-driven (mục 34) là nội bộ backend, không phải notification UI cho người dùng.                                                                   | Chưa có — grep `notification`, `bell` trong `apps/web/src/` không ra kết quả UI liên quan.                                                                                                                 |
| [X]        | Tiêu đề trang + dòng mô tả phụ (subheading)                                        | **Should** — mỗi module (mục 4,5,19,27...) có mục đích riêng được mô tả rõ; không có tên "page title" tường minh trong mục 37.                                                                        | Có nhưng có vấn đề — `Header.tsx` chỉ nhận `title`, KHÔNG có prop subtitle; đã ghi trong `docs/notes/bugs-and-findings.md` (mục cột "Điểm" Leaderboard, dòng "`Header.tsx` hiện không có slot subtitle").  |
| [ ]        | Dropdown chọn Pair/Coin (BTCUSDT...)                                               | **Must** — mục 37 "Binance data" + ví dụ tổng thể mục 3 dùng `Pair: BTCUSDT`.                                                                                                                         | Có, hoạt động đúng — `apps/web/src/market/PairSelect.tsx:1,8-23`, `PAIRS = ['BTCUSDT','ETHUSDT','SOLUSDT']`, dùng trong `RealtimeScreen.tsx:14,29`.                                                        |
| [ ]        | Nhóm nút chọn khung thời gian (1m/5m/15m/1h/4h...)                                 | **Must** — mục 37 "Tối đa 4 timeframe"; mục 5 mô tả nhóm nút `[1m][5m][15m][1h][4h][1d]`.                                                                                                             | Có, hoạt động đúng — `apps/web/src/market/TimeframeSelect.tsx:8-18`, dùng trong `RealtimeScreen.tsx:34`.                                                                                                   |
| [ ]        | Toggle bật/tắt Realtime                                                            | **No need** — đề bài không yêu cầu công tắc tắt realtime; mục 5 mô tả realtime luôn cập nhật, không có tuỳ chọn tắt.                                                                                  | Chưa có — `RealtimeScreen.tsx` không có toggle nào; kết nối luôn chạy ngầm qua `useTopic`/`useChannelStatus`.                                                                                              |
| [X]        | Badge trạng thái "Đang nhận dữ liệu" trên mỗi card                                 | **Should** — mục 32.7 Observability + 32.4 Reliability đòi hỏi biết trạng thái kết nối; không có tên cụ thể trong mục 37.                                                                             | Có, hoạt động đúng (dạng khác) — `useChannelStatus()` (`connecting`/`live`/`down`) + banner "Mất kết nối kênh" khi `isStale` (`CandleChart.tsx:203-204,226-231`).                                          |
| [ ]        | Lưới 4 ô biểu đồ candlestick đồng thời (multi-timeframe)                           | **Must** — mục 37 "Tối đa 4 timeframe" + mục 2.3 "theo dõi đồng thời tối đa 4 khung" + ví dụ lưới 2x2 ở mục 3.                                                                                        | Có, hoạt động đúng — `apps/web/src/market/Dashboard.tsx:16-49`, `DEFAULT_LAYOUT` 4 timeframe (`RealtimeScreen.tsx:11`), mỗi ô đổi timeframe độc lập.                                                       |
| [ ]        | Giá hiện tại + % thay đổi hiển thị trên mỗi card                                   | **Should** — mục 4 "Realtime Data" ví dụ giá đổi theo giây; nội dung tự nhiên của mục 2.2, không có tên tường minh trong mục 37.                                                                      | Chưa có — `CandleChart.tsx` không render giá hiện tại/% thay đổi dạng số riêng lẻ, chỉ có chart nến; giá "hiện tại" chỉ suy ra từ nến cuối.                                                                |
| [ ]        | Badge tín hiệu BUY/SELL trên mỗi card                                              | **Must** — mục 37 "Visualization: Chart có Buy/Sell"; mục 5 liệt "Buy Signal, Sell Signal". Áp dụng với chart trong Backtest.                                                                         | Có — Buy/Sell tồn tại ở tầng backtest (`SingleRunChart.tsx`), KHÔNG có ở Realtime: `Annotations.tsx` trả về `null`, comment *"Reserved... Nothing to show until then"* — xác nhận chưa nối vào Realtime.   |
| [X]        | MA(20) overlay trên mỗi biểu đồ Realtime                                           | **Should** — mục 5 "Có thể visualize: ... MA"; không có tên tường minh trong mục 37.                                                                                                                  | Chưa có ở Realtime — `CandleChart.tsx:135-156` chỉ có Candlestick + Volume series, không có MA; MA overlay chỉ tồn tại ở `/backtest` (`SingleRunChart.tsx`).                                               |
| [ ]        | Volume bar dưới mỗi candlestick                                                    | **Should** — mục 5 "Có thể visualize: Candlestick, Volume..."; không có tên tường minh trong mục 37 (MVP chỉ ghi "Candlestick chart").                                                                | Có, hoạt động đúng — `CandleChart.tsx:143-150,71-77`, `HistogramSeries` từ `toVolumeBar()`.                                                                                                                |
| [ ]        | Nút "Load 1000 nến lịch sử"                                                        | **No need** ở trang Realtime — theo ADR 0040/0041 (đã ghi trong `docs/notes/bugs-and-findings.md`), backfill 1000 nến không còn gắn với việc xem Realtime, chỉ gắn với lúc tạo Dataset ở `/backtest`. | Chưa có — khớp bugs-and-findings.md: `candle-backfill.service.ts` chỉ được gọi từ `apps/api/src/search/backtest.service.ts`, không liên quan trang Realtime.                                               |
| [ ]        | Panel "Logic cập nhật candle" (giải thích update-vs-append cho người xem)          | **No need** — nội dung mang tính giải thích cho người xem mockup, không phải UI feature nào được đề bài yêu cầu.                                                                                      | Chưa có dạng panel UI — cơ chế tương ứng có tồn tại trong code (`CandleChart.tsx:79-83` hàm `appendCandle()`) nhưng chỉ là logic nội bộ, không hiển thị ra UI.                                             |
| [X]        | Panel "Trạng thái kết nối" (đã kết nối/nguồn dữ liệu/latency/dữ liệu cuối/ổn định) | **Should** — mục 32.3 Realtime + 32.4 Reliability là driver trực tiếp đòi hỏi quan sát trạng thái kết nối; không có tên tường minh trong mục 37.                                                      | Có nhưng có vấn đề (thiếu chi tiết) — chỉ có trạng thái tổng quát 3 mức (`use-topic.ts`) + banner lỗi; KHÔNG có latency (ms), KHÔNG có "dữ liệu cuối" dạng field riêng, KHÔNG hiển thị dòng nguồn dữ liệu. |
| [ ]        | Panel "Recent Ticks" (thời gian/giá/khối lượng/loại Buy-Sell)                      | **Should** — mục 4 mô tả đúng dạng dữ liệu tick-by-tick; không có tên tường minh "bảng tick" trong mục 37.                                                                                            | Có, hoạt động đúng — `apps/web/src/market/RecentTicks.tsx:19-87`, 4 cột đúng như mockup, `KEEP = 5` dòng gần nhất.                                                                                         |
| [X]        | Panel "Chú thích" (legend màu nến/BUY-SELL/MA/Volume) trong Realtime.              | **No need** — không mục nào yêu cầu 1 panel chú thích riêng; chi tiết trình bày AI thêm cho dễ hiểu ảnh mockup.                                                                                       | Chưa có — không có component "Chú thích"/"Legend" nào trong `RealtimeScreen.tsx` (chỉ có `RecentTicks` và `Annotations` ở cột phải).                                                                       |


---

## Màn hình 2: Tạo Strategy từ Prompt / URL


| Quyết định | Tính năng                                                                               | Mức độ yêu cầu (căn cứ đề bài)                                                                                                                                                                                                                                                                                                                                                                        | Hiện trạng hệ thống (bằng chứng)                                                                                                                                                                                                                                                                                                                              |
| ---------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ ]        | Nhập mô tả strategy bằng ngôn ngữ tự nhiên (textarea, đếm ký tự)                        | **No need** — đề bài không nhắc "prompt"/"natural language"/textarea mô tả strategy ở bất kỳ mục nào. Mục 38 chỉ có "LLM Strategy Generator" trong nhóm *Search* (sinh candidate trong vòng loop tìm kiếm tự động) — khác với việc người dùng gõ tay mô tả để tạo 1 strategy đơn; không phục vụ driver Modifiability ở mục 32.1/40.1 (nói về `register(NewStrategy)` bằng code, không qua UI prompt). | Chưa có — grep "prompt" (case-insensitive) toàn repo chỉ trúng `groq-sentiment.provider.ts` (prompt cho LLM chấm sentiment, không liên quan strategy). Không có route/service/component nào cho strategy-prompt. `apps/web/src/screens/` chỉ có `BacktestScreen, LeaderboardScreen, NewsScreen, RealtimeScreen, SearchScreen` — không có màn tương ứng ảnh 2. |
| [ ]        | Nút "Phân tích bằng LLM" → sinh strategy JSON (indicators/conditions/riskManagement)    | **No need** — không mục nào mô tả pipeline "LLM đọc mô tả → sinh JSON strategy". Mục 6 chỉ định nghĩa interface `Strategy.analyze(context)` do lập trình viên code tay.                                                                                                                                                                                                                               | Chưa có — grep `\bllm\b` case-insensitive trên toàn bộ `apps/api/src`, `apps/web/src` trả về **0 kết quả**.                                                                                                                                                                                                                                                   |
| [ ]        | Nhập URL chiến lược + nút "Trích xuất từ website"                                       | **No need** — đề bài không có khái niệm "import strategy từ URL bên ngoài" ở đâu cả; không driver nào ở mục 32/40 cần đến nó.                                                                                                                                                                                                                                                                         | Chưa có — grep "extract" chỉ trúng `rss-news.provider.ts`, `cryptocompare-news.provider.ts` (thuộc News Crawler, không liên quan Strategy). Không có endpoint import-URL nào.                                                                                                                                                                                 |
| [ ]        | Hiển thị "Strategy đã phân tích" dạng structured (LONG/SHORT/risk/timeframe/pairs)      | **No need** — hệ quả trực tiếp của luồng phân tích LLM/URL không tồn tại; đề bài không mô tả UI xem trước kết quả phân tích.                                                                                                                                                                                                                                                                          | Chưa có — không có component/màn hình tương ứng (cùng bằng chứng ở 2 dòng trên).                                                                                                                                                                                                                                                                              |
| [ ]        | Khối "Định nghĩa strategy (JSON)" hiển thị đầy đủ + nút Sao chép                        | **Should** — cấu trúc strategy definition (indicators, conditions, riskManagement, timeframe) khớp dữ liệu strategy hệ thống phải quản lý (mục 6 `interface Strategy`, mục 35 "Strategy: StrategyDefinition, Parameters, Version"), dù đề bài không yêu cầu UI JSON editor cụ thể.                                                                                                                    | Có nhưng khác hình thức — `GET /strategies` (`apps/api/src/strategy/strategy.controller.ts:9-12`) trả `StrategyMeta[]` (metadata + tham số mặc định) qua `StrategyRegistry.list()`, không phải JSON "definition" đầy đủ như mockup, và không có UI hiển thị code block + nút sao chép.                                                                        |
| [ ]        | Validation strategy (thiếu trường/logic/chỉ báo hỗ trợ/trạng thái hợp lệ)               | **No need** — hệ quả của luồng tạo qua prompt/URL không tồn tại; đề bài không yêu cầu validate strategy nhập tay qua UI.                                                                                                                                                                                                                                                                              | Chưa có — không có endpoint/logic validate input strategy từ người dùng; các `*.strategy.ts` viết cứng và đăng ký qua `registeredStrategies` (`apps/api/src/strategy/registered-strategies.ts:10-17`), không qua validation runtime cho input động.                                                                                                           |
| [ ]        | Lưu vào "Strategy Library" (Name/Version/Tags/Source)                                   | **Should** (riêng khái niệm Version + đăng ký) — mục 36 "Strategy phải có Version" + mục 12 "register(SupportResistance)" mô tả yêu cầu kiến trúc tương ứng, nhưng KHÔNG mô tả 1 form "Save to Library" với Tags/Source do người dùng nhập — đề bài hình dung version/registration là việc lập trình viên.                                                                                            | Có 1 phần, khác cơ chế — mỗi `*.strategy.ts` có field `version` tĩnh trong code (VD `ma.strategy.ts:18` — `version: 1`), đăng ký qua mảng `registeredStrategies` (build-time), KHÔNG có bảng DB "Strategy Library" động, không Tags/Source, không form nhập tay.                                                                                              |
| [ ]        | Bảng "Chiến lược đã import gần đây" (Source/Ngày tạo/Version/Tags/Trạng thái/Hành động) | **No need** — phụ thuộc hoàn toàn vào luồng import không tồn tại trong đề bài.                                                                                                                                                                                                                                                                                                                        | Chưa có — không có repository/table lưu lịch sử "strategy đã import"; route duy nhất liên quan (`GET /strategies`) không trả dữ liệu dạng bảng lịch sử import.                                                                                                                                                                                                |


---

## Màn hình 3: Strategy Engine &amp; Loop Discovery


| Quyết định | Tính năng                                                                       | Mức độ yêu cầu (căn cứ đề bài)                                                                                                                                                                                                     | Hiện trạng hệ thống (bằng chứng)                                                                                                                                                                                                                                                                                                           |
| ---------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [ ]        | Danh sách strategy đơn có sẵn dạng card chọn (MA/RSI/Bollinger/S-R/SMC/Wyckoff) | **Must** cho MA/RSI/Bollinger/S-R — mục 37 "Strategy: ít nhất 4 strategy đơn lẻ". **Should** cho SMC/Wyckoff — mục 11: "không bắt buộc xây đầy đủ... mục tiêu là chứng minh kiến trúc có khả năng hỗ trợ" (không ở mục 37 hay 38). | Có nhưng có vấn đề — `apps/api/src/strategy/registered-strategies.ts:10-17` chỉ đăng ký MA, MACD, MACD v2, RSI, Bollinger, Support-Resistance, Sentiment — **không có SMC, không có Wyckoff** (grep `"SMC                                                                                                                                  |
| [ ]        | Nút "+ Tạo strategy đơn mới" (form tạo strategy tuỳ chỉnh qua UI)               | **No need** — mục 12 mô tả rõ "thêm strategy mới" là việc developer viết class + `StrategyRegistry.register()`, không phải 1 form end-user; không nơi nào mô tả nút UI này (khác màn "Tạo từ Prompt/URL" đã xét ở Màn hình 2).     | Chưa có — `strategy.controller.ts:9` chỉ có `@Get()`, không có `@Post()`/`@Put()` để tạo/đăng ký strategy qua API.                                                                                                                                                                                                                         |
| [X]        | Multi-select "Chọn các strategy để kết hợp" (tự build composite thủ công)       | **Should** — mục 13 "Composite Strategy" + ví dụ mục 3 và demo mục 46 bước 2 ("bật MA, RSI, Bollinger, Support/Resistance").                                                                                                       | Chưa có đúng dạng mockup — `StrategySpacePicker.tsx` chỉ chọn **không gian tìm kiếm** cho Loop Discovery tự sinh tổ hợp, không phải build 1 composite cố định. `customSpec` (`BacktestScreen.tsx:67,98`) chỉ được set khi click 1 dòng leaderboard có sẵn, không có UI tự chọn+gán trọng số từ đầu.                                        |
| [ ]        | Preset kết hợp nhanh (nút "MA+RSI", "RSI+Bollinger"...)                         | **No need** — không mô tả nào về nút preset; mục 3 chỉ liệt kê tổ hợp làm ví dụ minh hoạ kết quả, không phải yêu cầu UI control.                                                                                                   | Chưa có — grep "preset" trong `apps/web/src/search`, `apps/web/src/backtest` không ra kết quả.                                                                                                                                                                                                                                             |
| [X]        | Weighted Voting — bảng trọng số + checkbox bật/tắt (chỉnh sửa qua UI)           | **Should** — mục 14 "Weighted Combination" mô tả chi tiết gán trọng số riêng từng strategy + công thức tính score.                                                                                                                 | Chưa có dạng chỉnh sửa UI — trọng số tự động tính bởi `balancedWeights()` (`apps/api/src/search/candidate-space.ts:23-26`), không có slider/checkbox cho người dùng tự set. Hiển thị duy nhất là đọc-only ở `StrategyPicker.tsx:104-109` — đã ghi trong `docs/notes/bugs-and-findings.md` (mục "chi tiết tham số hiển thị dạng JSON thô"). |
| [ ]        | "Tín hiệu tổng hợp hiện tại" LONG/HOLD/SHORT + score (xem trước, chưa backtest) | **Should** — mục 6 (chuẩn hoá tín hiệu) + mục 14 (ví dụ tính score → quy về BUY/SELL/HOLD theo ngưỡng).                                                                                                                            | Chưa có dạng panel xem trước độc lập — logic tính hướng tồn tại (`apps/api/src/strategy/runnable-strategy.ts:37-43`) nhưng chỉ chạy trong 1 lượt backtest lịch sử, không có widget hiển thị tín hiệu "hiện tại" (live) ở `SearchScreen.tsx`/`BacktestScreen.tsx`.                                                                          |
| [ ]        | Ngưỡng vào lệnh có thể cấu hình (`                                              | score                                                                                                                                                                                                                              | ≥ x`)                                                                                                                                                                                                                                                                                                                                      |
| [ ]        | Nút "Backtest ngay" từ màn kết hợp                                              | **Should** — Backtest là Must ở mục 37/mục 19 nói chung; riêng UI shortcut từ màn combine không có mô tả cụ thể.                                                                                                                   | Có nhưng khác vị trí mockup — đường chạy backtest cho 1 spec composite có thật (click dòng leaderboard → `customSpec` → `BacktestScreen.tsx:98,128-129`), nhưng không có nút riêng trong 1 màn "Strategy kết hợp" vì màn build-composite-thủ-công chưa tồn tại.                                                                            |
| [ ]        | Nút "Lưu strategy kết hợp" (lưu composite đặt tên vào thư viện)                 | **Should** — mục 36 "Strategy phải có Version" + mục 35 (Database nhóm Strategy: Definition/Parameters/Version/CreatedAt).                                                                                                         | Chưa có — `strategy.repository.ts:12-38` chỉ đăng ký strategy đơn từ code lúc boot, không có route/bảng lưu 1 composite do người dùng đặt tên tuỳ ý.                                                                                                                                                                                       |
| [ ]        | Sơ đồ pipeline Loop Discovery (Generate→Backtest→Evaluate→Rank→Leaderboard)     | **Should** — mục 23 "Continuous Strategy Loop" mô tả đúng chuỗi 5 bước bằng sơ đồ ASCII tương tự.                                                                                                                                  | Có, hoạt động đúng (là luồng chức năng thật, không phải hình vẽ) — `backtest-worker.module.ts` (BullMQ+Redis) → `BacktestRunnerService` → `EvaluationModule` → cập nhật leaderboard qua `ranking`. Không có SVG minh hoạ pipeline trên UI, nhưng luồng 5 bước có thật.                                                                     |
| [X]        | Leaderboard Top strategies (Rank/Strategy/Profit/Winrate)                       | **Must** — mục 37 "Leaderboard: Top-K strategies".                                                                                                                                                                                 | Có nhưng có vấn đề — đã ghi trong `docs/notes/bugs-and-findings.md`: `LeaderboardTable.tsx:188-190,216` gộp tên hiển thị `strategy(weight%)` không hiện tham số → nhiều dòng trông giống hệt nhau; cột "Điểm" không có tooltip giải thích cách tính.                                                                                       |
| [ ]        | Phương pháp Discovery — Random Search                                           | **Must** — mục 37 "Search: ít nhất Random Search".                                                                                                                                                                                 | Có, hoạt động đúng — `random-candidate.generator.ts`, đăng ký trong `generated-candidate-source.ts:21`.                                                                                                                                                                                                                                    |
| [ ]        | Phương pháp Discovery — Domain-guided Search                                    | **Should** — mục 17 mô tả chi tiết (nhóm Trend/Momentum/Structure...).                                                                                                                                                             | Có, hoạt động đúng — `domain-guided-candidate.generator.ts`, DTO `start-run.dto.ts:26` chấp nhận `mode: 'random'                                                                                                                                                                                                                           |
| [ ]        | Phương pháp Discovery — Genetic Search                                          | **Nice to have** — mục 18 liệt "Genetic Algorithm" trong "cách tìm kiếm nâng cao" và kết luận "Đây là phần mở rộng, không bắt buộc"; mục 38 cũng liệt lại.                                                                         | Chưa có — grep "genetic" toàn repo chỉ ra comment/test minh hoạ, không có class/route implement; `start-run.dto.spec.ts:34` xác nhận mode `'bayesian'` bị từ chối — hệ thống chỉ hỗ trợ 2 mode.                                                                                                                                            |
| [ ]        | Tiến trình Discovery real-time (iteration/tested/best-so-far)                   | **Should** — mục 24 &amp; 32.7 (Observability: "đã thử bao nhiêu strategy? đứng Top 1?"); mục 46 demo bước 4 mô tả đúng dạng này.                                                                                                  | Có, hoạt động đúng (gần khớp mockup) — `SearchProgressPanel.tsx:96-133` hiện "Đã thử", "Đang chờ", "Thất bại", "Backtest trung bình", candidate đang chạy, "Kết quả tốt nhất hiện tại" (chỉ thiếu tên tổ hợp+winrate đầy đủ cho best-so-far — chưa tới mức bug riêng).                                                                     |


---

## Màn hình 4: Backtest &amp; Kết quả giao dịch


| Quyết định | Tính năng                                                                   | Mức độ yêu cầu (căn cứ đề bài)                                                                                                                                                                                                                                                                                                          | Hiện trạng hệ thống (bằng chứng)                                                                                                                                                                                                                                                                                                                                         |
| ---------- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [ ]        | Cấu hình chạy backtest: Pair/Coin, Timeframe, khoảng ngày From-To, Strategy | **Must** — mục 37 "Backtest: có khả năng giả lập giao dịch trên historical data"; mục 19 mô tả rõ cần khoảng ngày/pair/timeframe.                                                                                                                                                                                                       | Có, nhưng khác cấu trúc mockup — không phải 1 màn hình duy nhất; Pair/Timeframe/From/To/entryPrice/feeRate cấu hình khi **tạo Dataset** (`DatasetFormModal.tsx:15-29`), màn `/backtest` chỉ chọn Dataset đã tạo sẵn (`DatasetPicker.tsx:27-60`) + chọn Strategy (`StrategyPicker.tsx`).                                                                                  |
| [ ]        | Trường "Vốn (USD)" — vốn ban đầu để backtest                                | **No need** — không có từ "vốn"/"capital" ở bất kỳ mục nào; mọi ví dụ (mục 3,19,33) thể hiện kết quả bằng % (return), không quy đổi qua vốn ban đầu.                                                                                                                                                                                    | Chưa có — grep `"capital                                                                                                                                                                                                                                                                                                                                                 |
| [ ]        | Transaction Cost (%) — phí giao dịch cấu hình được                          | **No need** — không có từ "phí"/"fee"/"transaction cost" ở bất kỳ đâu trong đề bài, kể cả mục 38 (nhóm Trading không có phí giao dịch).                                                                                                                                                                                                 | Có, hoạt động đúng (dù không được yêu cầu) — `feeRate` là trường của `BacktestRules`, validate ở `dataset-validator.ts:86-91`, nhập qua `DatasetFormModal.tsx:26`, áp dụng thật ở `backtest-runner.service.ts:161-166` (trừ cả 2 chiều entry/exit).                                                                                                                      |
| [ ]        | Slippage (bps) — trượt giá cấu hình được                                    | **No need** — không xuất hiện ở bất kỳ đâu, kể cả mục 38.                                                                                                                                                                                                                                                                               | Chưa có — grep `"slippage"` toàn repo = **0 kết quả**; công thức lãi ròng (`backtest-runner.service.ts:158-166`) chỉ trừ `feeRate`, không có slippage.                                                                                                                                                                                                                   |
| [X]        | Biểu đồ nến (candlestick) kết quả backtest                                  | **Must** — mục 37 "Market: Candlestick chart".                                                                                                                                                                                                                                                                                          | Có, hoạt động đúng — `apps/web/src/backtest/SingleRunChart.tsx`. Cùng bug UTC timezone đã ghi trong `docs/notes/bugs-and-findings.md` (liệt `SingleRunChart.tsx` là 1 trong 2 nơi bị ảnh hưởng) — không lặp lại chi tiết.                                                                                                                                                |
| [ ]        | Đường MA(20)/MA(50) overlay trên chart                                      | **Should** — mục 5 "có thể visualize: MA"; mục 25 ví dụ có "MA20" — không có tên trong mục 37 MVP (Visualization MVP chỉ nói Buy/Sell + Entry/Exit).                                                                                                                                                                                    | Có, hoạt động đúng — `SingleRunChart.tsx:145-172` vẽ "MA Fast"/"MA Slow"/"MA(20)" khi indicators tương ứng tồn tại.                                                                                                                                                                                                                                                      |
| [ ]        | Đường/vùng Support &amp; Resistance trên chart                              | **Should** — mục 5 "vùng Support... vùng Resistance"; mục 10 mô tả chi tiết — không có tên trong mục 37 MVP.                                                                                                                                                                                                                            | Có, hoạt động đúng — `SingleRunChart.tsx:211-240` vẽ "Support"/"Resistance" khi strategy S/R nằm trong tổ hợp đang chạy.                                                                                                                                                                                                                                                 |
| [ ]        | Marker Entry (điểm vào lệnh) trên chart                                     | **Must** — mục 37 "Visualization: Chart có Buy/Sell, Entry/Exit".                                                                                                                                                                                                                                                                       | Có, hoạt động đúng — `SingleRunChart.tsx:254-262`, marker `arrowUp`/`arrowDown`, nhãn "Mua/Bán #seq".                                                                                                                                                                                                                                                                    |
| [ ]        | Marker Take Profit / Stop Loss trên chart                                   | **Nice to have** — tên tường minh trong mục 38 nhóm Trading ("Stop Loss, Take Profit"), KHÔNG có trong mục 37 MVP. (Mục 5 nhắc SL/TP trong danh sách khả năng chung của chart, không phải yêu cầu bắt buộc cơ chế SL/TP).                                                                                                               | Chưa có — grep `"stoploss                                                                                                                                                                                                                                                                                                                                                |
| [ ]        | Marker Exit (điểm thoát lệnh) trên chart                                    | **Must** — mục 37 "Entry/Exit".                                                                                                                                                                                                                                                                                                         | Có, hoạt động đúng — `SingleRunChart.tsx:265-273`, marker `circle`/`square`, nhãn "Thoát #seq".                                                                                                                                                                                                                                                                          |
| [ ]        | Volume bar dưới chart                                                       | **Should** — mục 5 "có thể visualize: Volume" — không có tên trong mục 37 MVP.                                                                                                                                                                                                                                                          | Có, hoạt động đúng — `SingleRunChart.tsx:43-48,96-126`.                                                                                                                                                                                                                                                                                                                  |
| [ ]        | Bảng danh sách lệnh giao dịch (thời gian/giá vào-ra, chiều, lãi ròng)       | **Should** — mục 26 "Trade Detail" mô tả nguyên văn bảng dạng #/Entry Time/Entry/Exit Time/Exit/Result — ở thân bài, không có tên tường minh trong mục 37.                                                                                                                                                                              | Có, nhưng thiếu cột so với mockup — `TradesTable.tsx:37-46` có đủ #, Chiều lệnh, Thời gian/Giá vào-ra, Lãi ròng — không có cột Stoploss/TakeProfit/Phí/Slippage (nhất quán với các dòng "No need"/"Chưa có" ở trên).                                                                                                                                                     |
| [ ]        | Phân trang bảng lệnh giao dịch                                              | **No need** — không có mô tả nào về phân trang UI trong toàn bộ đề bài.                                                                                                                                                                                                                                                                 | Chưa có — `TradesTable.tsx:34` dùng cuộn (`maxHeight: 280px, overflowY: auto`), không có control phân trang.                                                                                                                                                                                                                                                             |
| [ ]        | Metric cards: Return / Win Rate / Max Drawdown / Number of Trades           | **Must** — mục 37 "Evaluation tối thiểu: Return, Win Rate, Max Drawdown, Number of Trades".                                                                                                                                                                                                                                             | Có, hoạt động đúng — `MetricsPanel.tsx:38-79`, tính bằng `apps/api/src/evaluation/calculators/{win-rate,drawdown}.calculator.ts`.                                                                                                                                                                                                                                        |
| [ ]        | Metric cards: Profit Factor, Sharpe Ratio                                   | **Should** — mục 20 liệt kê rõ trong thân bài (ngoài 4 chỉ số MVP).                                                                                                                                                                                                                                                                     | Có, hoạt động đúng — `risk-metrics.calculator.ts:8-45`, hiển thị ở `MetricsPanel.tsx:81-113`.                                                                                                                                                                                                                                                                            |
| [ ]        | Metric cards tách riêng "Wins"/"Losses" (số lệnh tuyệt đối)                 | **No need** — đề bài chỉ nhắc "Win Rate" (%, mục 20/37), không yêu cầu số lệnh thắng/thua tuyệt đối riêng.                                                                                                                                                                                                                              | Chưa có (chỉ tính nội bộ) — `win-rate.calculator.ts:9` tính `wins` nội bộ nhưng chỉ trả về tỷ lệ; `MetricsPanel.tsx` không có ô "Wins"/"Losses" riêng.                                                                                                                                                                                                                   |
| [ ]        | Giải thích công thức Profit (Gross − Fee − Slippage = Net) trên UI          | **No need** — không mục nào yêu cầu hiển thị breakdown lãi/lỗ trên UI; vế Slippage vốn đã "No need" nên càng không có căn cứ.                                                                                                                                                                                                           | Chưa có — không có panel breakdown nào trong `apps/web/src/backtest/`; công thức thật (`grossReturn - totalFee`, không slippage) chỉ tồn tại trong backend, không hiển thị UI.                                                                                                                                                                                           |
| [ ]        | Panel "Giả định Backtest" (LONG+SHORT / SL-TP theo OHLC / reproducible)     | Hỗn hợp: bản thân panel checklist = **No need** (không mục nào yêu cầu 1 khối "giả định" hiển thị UI). Nội dung: "LONG+SHORT" = **Nice to have** (mục 38 nhóm Trading); "SL/TP theo OHLC" = **Nice to have** (mục 38, và hệ thống chưa có SL/TP — xem dòng trên); "Reproducible" = **Should** (mục 36 nêu đích danh "Reproducibility"). | Panel UI: Chưa có — không có khối liệt kê giả định trong `apps/web/src/backtest/*.tsx`. LONG+SHORT: Có, hoạt động đúng, short thật (`backtest-runner.service.ts:152-157` đảo công thức lãi cho SELL). SL/TP theo OHLC: Chưa có. Reproducible: Có, hoạt động đúng — Dataset immutable (`dataset.repository.ts:29-56`, khớp ADR 0010 đã ghi trong `bugs-and-findings.md`). |


---

## Màn hình 5: News Crawler &amp; Phân tích thị trường


| Quyết định | Tính năng                                                                                   | Mức độ yêu cầu (căn cứ đề bài)                                                                                                                                                                                               | Hiện trạng hệ thống (bằng chứng)                                                                                                                                                                                                                                                                                                                         |
| ---------- | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ ]        | Chọn nguồn thu thập tin (nút Website/RSS/HTML)                                              | **Should** — mục 28: "Nên có: News Provider ← RSS                                                                                                                                                                            | News API                                                                                                                                                                                                                                                                                                                                                 |
| [ ]        | Lọc theo Pair/Asset (BTC/ETH/SOL...)                                                        | **Should** — mục 27 định nghĩa field `relatedCoins`; mục 29-30 sentiment/strategy đều gắn theo coin.                                                                                                                         | Có, hoạt động đúng — `NewsControls.tsx:13` (`COIN_OPTIONS`), truyền qua query cho `/api/news` và `/api/sentiment/stats` (`NewsScreen.tsx:29,49`).                                                                                                                                                                                                        |
| [ ]        | Auto refresh theo chu kỳ cấu hình (1-5 phút)                                                | **No need** — không có khái niệm "auto refresh"/polling interval cho News ở bất kỳ mục nào trong toàn văn đề bài.                                                                                                            | Chưa có — grep `auto.?refresh                                                                                                                                                                                                                                                                                                                            |
| [ ]        | Bắt đầu crawl thủ công (nút Collect)                                                        | **Must** — mục 37 "News": bước đầu tiên bắt buộc "Collect".                                                                                                                                                                  | Có, hoạt động đúng — nút "Thu thập tin tức" → `POST /api/news/collect` (`NewsScreen.tsx:67-94`, `news.controller.ts:66-102`).                                                                                                                                                                                                                            |
| [ ]        | Chạy phân tích sentiment cho batch bài viết (nút Analyze)                                   | **Must** — mục 37 "News": bước "Analyze sentiment" bắt buộc trong MVP.                                                                                                                                                       | Có, hoạt động đúng — nút "Chạy phân tích sentiment AI" → `POST /api/sentiment/batch` (`NewsScreen.tsx:96-123`, `sentiment.controller.ts:32-43`).                                                                                                                                                                                                         |
| [ ]        | Danh sách tin tức đầu vào (asset/tiêu đề/nguồn/thời gian)                                   | **Should** — mục 27 định nghĩa đầy đủ field `NewsItem`; mục 46 demo bước 8 hiển thị "BTC News".                                                                                                                              | Có, hoạt động đúng — `apps/web/src/news/NewsFeed.tsx:113-166` hiển thị source, thời gian, coin pills, tiêu đề, badge sentiment.                                                                                                                                                                                                                          |
| [ ]        | LLM-assisted Extraction — pipeline 4 bước (HTML thô→LLM hiểu tag→sinh template→lưu version) | **No need** — không xuất hiện ở bất kỳ mục nào (đã đọc toàn văn 1-47); mục 44 "Anti-pattern" còn cảnh báo ngược lại: "Crawler phụ thuộc chặt vào ML" — Crawler chỉ nên `collect news`, không tự hiểu cấu trúc HTML bằng LLM. | Chưa có — grep `template                                                                                                                                                                                                                                                                                                                                 |
| [ ]        | Quản lý version của extraction template                                                     | **No need** — không có căn cứ (mục 36 "Strategy phải có Version" nói về Strategy, không phải extraction template của Crawler — đã kiểm tra kỹ không liên quan).                                                              | Chưa có — cùng bằng chứng grep ở trên.                                                                                                                                                                                                                                                                                                                   |
| [ ]        | Self-healing extraction (tự phát hiện lỗi cao, tự sửa template)                             | **No need** — không có căn cứ; mục 44 khuyến cáo ngược lại.                                                                                                                                                                  | Chưa có — cùng bằng chứng grep ở trên.                                                                                                                                                                                                                                                                                                                   |
| [ ]        | Chỉ số chất lượng extraction (fields rỗng/sai định dạng/độ tin cậy/tổng lỗi)                | **No need** — không có căn cứ trong đề bài.                                                                                                                                                                                  | Chưa có — không có field/metric tương ứng trong model `News` (`prisma/schema.prisma`) hay service liên quan.                                                                                                                                                                                                                                             |
| [ ]        | Sentiment tổng hợp (Positive/Neutral/Negative %) + điểm trung bình                          | **Should** — mục 29 mô tả rõ 3 nhãn; mục 46 demo bước 8 liệt kê tường minh "Positive: 42% / Neutral: 38% / Negative: 20%".                                                                                                   | Có, đúng nhưng khác chi tiết mockup — `SentimentDistribution.tsx:60-107` tính %+`averageScore` qua `GET /sentiment/stats`. Khác mockup: đây là thống kê **toàn thời gian**, không phải cửa sổ **24h** như nhãn mockup — `sentiment.service.ts:140-144` `getStats(coin)` không nhận tham số thời gian.                                                    |
| [ ]        | Event Type classification (ETF/Fund Flow, Protocol Upgrade, Regulation...)                  | **No need** — không có căn cứ; mục 29 chỉ nói phân loại sentiment, không có phân loại "loại sự kiện".                                                                                                                        | Chưa có — grep `eventtype                                                                                                                                                                                                                                                                                                                                |
| [ ]        | Confidence Score trung bình (của việc trích xuất/LLM)                                       | **No need** — không có căn cứ trong đề bài.                                                                                                                                                                                  | Chưa có — grep `confidence` trong `apps/api/src/sentiment`, `apps/web/src/news` = 0 kết quả.                                                                                                                                                                                                                                                             |
| [ ]        | Độ bao phủ nguồn (số nguồn hoạt động/tổng)                                                  | **No need** — không có căn cứ trong đề bài.                                                                                                                                                                                  | Chưa có — không có metric/endpoint đếm "nguồn hoạt động" trong `apps/api/src/news`.                                                                                                                                                                                                                                                                      |
| [X]        | Tích hợp News Sentiment vào Strategy Engine (NewsSentimentStrategy)                         | **Should** — mục 30: "Sentiment có thể trở thành một Strategy" (`NewsSentimentStrategy`, rule mẫu); mục 46 demo bước 9-10 thêm `SentimentStrategy` vào search space.                                                         | Có nhưng có vấn đề — đã đăng ký thật: `apps/api/src/strategy/strategies/sentiment.strategy.ts` (id `'sentiment'`), tính điểm qua `sentiment.calculator.ts`. Vấn đề đã ghi trong `docs/notes/bugs-and-findings.md` ("Sentiment indicator KHÔNG lọc theo coin" — `indicator.repository.ts:9-21` không dùng field `relatedCoins`) — không lặp lại chi tiết. |


---

## Tổng hợp nhanh theo mức độ

- **Must chưa đạt đầy đủ / có vấn đề đáng chú ý:** Badge BUY/SELL chưa nối vào Realtime chart (chỉ có ở Backtest); Leaderboard Top-K có nhưng candidate trông giống hệt nhau do thiếu hiển thị tham số (đã ghi trong bugs-and-findings.md).
- **Should nổi bật đã có tốt:** 4 timeframe realtime, Random/Domain-guided Search, Metrics MVP + Profit Factor/Sharpe, Support/Resistance overlay, News collect→analyze pipeline, NewsSentimentStrategy.
- **Should còn thiếu:** UI build composite strategy thủ công (multi-select + lưu), weighted voting chỉnh sửa qua UI, ngưỡng vào lệnh cấu hình được, panel trạng thái kết nối đầy đủ (latency/nguồn dữ liệu), MA overlay ở Realtime, tín hiệu tổng hợp "xem trước" real-time.
- **Nice to have (mục 38) chưa có:** Genetic Search, Stop Loss/Take Profit thật (chỉ có Transaction Cost — vốn không được đề bài yêu cầu — chứ không có SL/TP).
- **No need — cụm lớn nhất, nhiều khả năng AI vẽ ảnh tự thêm:** toàn bộ luồng "Tạo Strategy từ Prompt/URL bằng LLM" (Màn hình 2), toàn bộ "LLM-assisted Extraction + Self-healing" của News Crawler, các thành phần trang trí chung (logo, badge gói cước, avatar, icon help/notification, badge nguồn dữ liệu), Vốn (USD)/Slippage ở Backtest, phân trang bảng lệnh, Event Type/Confidence Score/Độ bao phủ nguồn ở News.

